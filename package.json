{
  "name": "college-attendance-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that monitors college attendance and notifies a Discord webhook.",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.124.0"
  }
}
